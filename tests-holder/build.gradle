plugins {
    id 'groovy'
}

version 'unspecified'

repositories {
    mavenCentral()
}

dependencies {
    compile project(":drools-model")
    compile project(":drools-core")

    compile "org.freemarker:freemarker:$freemarkerVersion"
}
