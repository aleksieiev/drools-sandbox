plugins {
    id 'groovy'
}

version 'unspecified'

repositories {
    mavenCentral()
}

dependencies {
    compile project(":drools:lebowski-drools-model")
    compile project(":drools:lebowski-drools-core")

    compile "org.freemarker:freemarker:$freemarkerVersion"
}
